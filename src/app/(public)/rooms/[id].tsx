import { Screen } from '@/components/layout';

export default function RouteScreen() {
  return <Screen title='Room Details' description='Inspect a room before reserving or viewing availability.' />;
}