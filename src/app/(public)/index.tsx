import { Screen } from '@/components/layout';

export default function RouteScreen() {
  return <Screen title='BoardEase' description='Browse boarding houses, rooms, and public property details.' />;
}