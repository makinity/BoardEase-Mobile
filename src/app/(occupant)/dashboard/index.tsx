import { Screen } from '@/components/layout';

export default function RouteScreen() {
  return <Screen title='Occupant Dashboard' description='Review stay status, bills, reservations, and alerts.' />;
}