import { supabase } from '@/lib/supabase';

export async function getOwnerDashboard() {
  // TODO: implement owner dashboard summary query
  return { data: null, error: null };
}

export async function getOccupantDashboard() {
  // TODO: implement occupant dashboard summary query
  return { data: null, error: null };
}
